on:
    push:
      branches:
        - master2
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: 'Testing Github actions'
          run: pwd

        - name: 'Testing Printing'
          run: echo Ahmed